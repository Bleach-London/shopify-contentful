[build]
  # This will be run the site build
  command = "webpack && npm run build"
  # This is the directory is publishing to netlify's CDN
  publish = "webpack && build"
  # functions build directory
  functions = "functions-build"